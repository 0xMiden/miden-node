syntax = "proto3";
package errors;

// SUBMIT PROVEN TRANSACTION
// ================================================================================================

// SubmitProvenTransaction errors.
enum SubmitProvenTransactionError {
    SUBMIT_PROVEN_TRANSACTION_ERROR_UNSPECIFIED = 0;
    SUBMIT_PROVEN_TRANSACTION_ERROR_INTERNAL = 1;
    SUBMIT_PROVEN_TRANSACTION_ERROR_DESERIALIZATION_FAILED = 2;
    SUBMIT_PROVEN_TRANSACTION_ERROR_INVALID_TRANSACTION_PROOF = 3;
    SUBMIT_PROVEN_TRANSACTION_ERROR_INCORRECT_ACCOUNT_INITIAL_COMMITMENT = 4;
    SUBMIT_PROVEN_TRANSACTION_ERROR_INPUT_NOTES_ALREADY_CONSUMED = 5;
    SUBMIT_PROVEN_TRANSACTION_ERROR_UNAUTHENTICATED_NOTES_NOT_FOUND = 6;
    SUBMIT_PROVEN_TRANSACTION_ERROR_OUTPUT_NOTES_ALREADY_EXIST = 7;
    SUBMIT_PROVEN_TRANSACTION_ERROR_TRANSACTION_EXPIRED = 8;
}

// SUBMIT PROVEN BATCH
// ================================================================================================

// SubmitProvenBatch errors.
enum SubmitProvenBatchError {
    SUBMIT_PROVEN_BATCH_ERROR_UNSPECIFIED = 0;
    SUBMIT_PROVEN_BATCH_ERROR_INTERNAL = 1;
    SUBMIT_PROVEN_BATCH_ERROR_DESERIALIZATION_FAILED = 2;
}
